[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# SPA routing uchun redirect qoidasi
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

